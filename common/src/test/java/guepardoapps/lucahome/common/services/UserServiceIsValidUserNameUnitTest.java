package guepardoapps.lucahome.common.services;
/*
import guepardoapps.lucahome.common.services.user.UserService;

import org.junit.experimental.theories.DataPoints;
import org.junit.experimental.theories.Theories;
import org.junit.experimental.theories.Theory;
import org.junit.experimental.theories.suppliers.TestedOn;
import org.junit.runner.RunWith;

import static org.junit.Assert.assertEquals;

@RunWith(Theories.class)
public class UserServiceIsValidUserNameUnitTest {

    @Theory
    public void byteStringConversion_isCorrect(
            @TestedOn({value:""}) String userName,
            boolean expectedIsValid) throws Exception {
        boolean actualIsValid = UserService.Companion.getInstance().isValidUserName(userName);
        assertEquals(expectedIsValid, actualIsValid);
    }
}*/