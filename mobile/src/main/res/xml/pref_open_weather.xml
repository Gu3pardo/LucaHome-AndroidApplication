<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_open_weather_city"
        android:inputType="textCapWords"
        android:key="open_weather_city"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_enter_open_weather_city" />

    <SwitchPreference
        android:defaultValue="false"
        android:key="change_weather_wallpaper"
        android:title="@string/pref_title_change_weather_wallpaper" />

</PreferenceScreen>
